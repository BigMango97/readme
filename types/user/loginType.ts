export interface loginType{
    userId: string;
    accessToken: string;
    refreshToken: string;
    isLogin: boolean;
}
export interface userType{
    userId: string;
    accessToken: string;
    refreshToken: string;
    isLogin: boolean;
}